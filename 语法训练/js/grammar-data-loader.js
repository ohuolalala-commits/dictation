/* ============================================================
   ASD 语法训练系统 — 题库数据加载器
   合并 L1L2 + L3L4 + L5L6 + L7L8 的数据
   ============================================================ */

// 确保 grammarLevels 存在于 window，这样各数据文件可以共用
window.grammarLevels = window.grammarLevels || {};
const grammarLevels = window.grammarLevels;
