---
date: 2026-06-11
---

# 开发日志 — 2026年06月11日

## ✅ 完成事项

1. **新增"中考高频词"词库（236词）** — 数据来源于 WPS云盘 `陕西中考高频词汇.docx`
   - 从 docx 提取 236 个高频单词（PDF 为扫描件无法直接提取文字）
   - 含词性标注和中文释义（tip 字段保留完整定义）
   - 默认模式：中文→英文
   - 自动生成 hint（首字母 + 下划线）
   - 按字母顺序排列
   - 自动去重（源文档中有部分重复词条）

2. **更新 CLAUDE.md** — 词库清单中加入 `zk` 条目

## 🔧 技术细节

- PDF `中考高频词.pdf` 为纯图片扫描件（18页，每页1张图），无法用 pdfplumber/PyPDF2 提取文字
- 改用同源 Word 文档 `陕西中考高频词汇.docx` 提取数据
- 使用 Python 解析 Word COM 导出的文本，处理 `\x07` 分隔符
- 词性前缀剥离（V., n., adj., V/n 等），保留纯净中文提示
- 236 词去重后保留（源文档 247 词标称，含重复）

## 📋 待办事项

1. 中考高频词 `cn` 字段部分条目可进一步人工校对（约有 10-15 条的中文提示在去除词性标签后拼接不够完美）
2. 恢复/确认键盘音效在不同浏览器下的表现
3. specs/ 下各规范文件需细化填充
4. 考虑是否将桌面上的 `句子翻译.html` 也纳入此项目管理

## 🐛 问题与解决

1. **中文全部消失** — 新词库 `zk` 插入后，`pastTense` 闭合 `}` 后缺少逗号 `,`，导致 JS 语法错误，整个脚本挂掉，页面所有中文/功能均无法渲染。在 468 行 `}` 后补上 `,` 解决。

## 📝 备注

- 新增词库 ID：`zk`
- 词库 icon：📗
- 插入位置：`英语默写本.html` wordBanks 对象末尾
